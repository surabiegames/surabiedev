/** Tab Download — tarik paket rute & master tarif untuk kerja offline. */
import { UnduhDataScreen } from '@/features/baca-meter/unduh-screen';

export default function Unduh() {
  return <UnduhDataScreen />;
}
